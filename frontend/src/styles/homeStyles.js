import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  section: {
    marginBottom: 20,
    gap: 20,
    maxHeight: 250,
  },

  bookItem: {
    marginRight: 10,
  },

  centered: {
    alignItems: "center",
    justifyContent: "center",
    flexGrow: 1,
  },
});
